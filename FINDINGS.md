# Findings queue — verified by adversarial review (run wf_315cd7bd-fb1, 2026-08-12)

31 findings, all independently CONFIRMED against the code. P1 = the ten
reported as severe; P2 = the rest. Every entry gets: a test that FAILS on
the current code first, then the fix, then the class-sweep check (below).

## P1 — severe

- [x] (3508897) `src/todo/tui/list_view.py:649` — TUI crashes on launch with MarkupError when any item title contains Rich markup metacharacters, because row cells are passed to DataTable as plain strings which it parses as markup
      Scenario: Verified: `todo add "Fix [/] thing"` then `todo ui` crashes immediately on startup (DataTable.default_cell_formatter -> Text.from_markup -> MarkupError "closing tag '[/]' has nothing to close"). The TUI is completely unusable until the offending item is deleted via the CLI; any title with an unbalanced closing tag like "[/]" or "[/b]" triggers it. The is_blocked branch wraps cells in Text (safe) but the common unblocked branch passes raw str.
- [x] (3508897) `src/todo/adapters/output.py:156` — RichOutput.print_list (and print_project's description/log at line 319) render user text as Rich markup, so `todo list` on a TTY crashes for titles containing markup; the in-range 'markup crash fix' (_styled) only fixed the empty-style case
      Scenario: Verified: RichOutput.print_list with an item titled "Fix [/] thing" raises MarkupError (table cells are strings, parsed as markup). A user who runs `todo add "Fix [/] thing"` gets a traceback from every subsequent `todo list`/`todo summary` in a terminal until they edit the item, and `todo project show` crashes likewise on a description or log body containing `[/]`. Piped/plain output works, which hides the bug from all CLI tests (CliRunner is not a TTY) and from test_output_rich.py, which never uses markup-metachar text.
- [x] (67133e2) `src/todo/infra/cli/main.py:405` — project add/edit/archive/log re-resolve the project via show_project(storage, str(id)), which resolves by NAME first, so they display (and emit as --json) a different project whose name equals the numeric id
      Scenario: Verified: with an existing project named "2", `todo project add foo --json` succeeds but prints the JSON detail of project "2" (id 1) instead of the just-created "foo" (id 2). Any script consuming the output records the wrong project id/name; `project edit`, `project archive`, and `project log` have the same bug since they also round-trip the known integer id through the name-first resolver instead of calling show_project with the Project they already hold.
- [x] (859e83a) `src/todo/infra/cli/main.py:354` — `todo block ID B1 B2...` applies blockers one-by-one with no rollback, so a failure partway leaves earlier edges committed while reporting failure
      Scenario: Verified: `todo block 2 1 999` prints "Todo item #999 not found" and exits 1, but the 1-blocks-2 edge was already committed — `todo show 2 --json` now shows blocked_by [1] and is_blocked true. A user who sees the error, fixes the typo, and expects a clean state (or a script retrying the whole command) ends up with half-applied dependency state that the exit code claims never happened; the same loop shape affects `unblock` with multiple ids.
- [x] (67133e2) `src/todo/infra/cli/main.py:405` — project_add re-resolves the newly created project via show_project(storage, str(created.id)), and resolve_project prefers name lookup over id, so a project literally named after the new project's numeric id is displayed instead of the one just created.
      Scenario: Verified by repro: with a project named "2" (id 1) already present, `todo project add foo --json` succeeds in creating "foo" (id 2) but prints the JSON/detail of project id 1 named "2" — a script consuming the output records the wrong project id/name. The same name-shadows-id resolution makes `todo project show 2`, edit, archive, log, and rm target the wrong project, so `todo project rm 2` intending id 2 would delete a different project (data loss).
- [x] (859e83a) `src/todo/infra/cli/main.py:354` — The block command applies blocker edges one at a time inside the loop and exits on the first error, leaving earlier edges committed even though the command reports failure.
      Scenario: Verified by repro: `todo block 3 1 999` prints "Todo item #999 not found" and exits 1, but item 3 is now silently blocked by item 1 (`show 3 --json` reports blocked_by [1]). A user or script that treats exit 1 as "nothing happened" and retries or abandons the operation ends up with half-applied dependency state; the same partial commit happens when a later blocker in the list would form a cycle.
- [x] (859e83a) `src/todo/adapters/sqlite_storage.py:149` — Schema setup lost its old idempotence guard: migrations run CREATE TABLE without IF NOT EXISTS and the PRAGMA user_version bump is a separate non-atomic step after executescript, so a partially-applied migration state is fatal on every subsequent open.
      Scenario: A crash/kill/disk-full between `executescript(script)` (which autocommits DDL statement-by-statement) and the `PRAGMA user_version` bump leaves the projects table created with user_version still 0. Reproduced: opening such a DB makes every `todo` command die with an uncaught traceback `sqlite3.OperationalError: table projects already exists` — the database is permanently unusable until manually repaired. The pre-change init (all CREATE TABLE IF NOT EXISTS) was safe to re-run on any state.
- [x] (67133e2) `src/todo/adapters/sqlite_storage.py:306` — done_since() never hydrates blocker relations, so summary output reports blocked_by/blocking as empty even when the get()/list() paths report real edges for the same item.
      Scenario: Reproduced: with item #1 done and still recorded as blocking #2, `todo show 1 --json` prints "blocking": [2] but `todo summary --since '1 days' --json` prints "blocking": [] and "blocked_by": [] for the same item — JSON consumers of the summary get factually wrong dependency data that contradicts the rest of the CLI.
- [x] (67133e2) `src/todo/adapters/output.py:351` — PlainOutput.print_list no longer prints titles verbatim: blocked items get a '\U0001f6a7 ' (🚧) prefix injected into the plain formatter that exists specifically for machine-friendly piped output.
      Scenario: A script that pipes `todo list` (non-TTY selects PlainOutput) and greps/parses the title column matches an item while it is unblocked but stops matching the moment another item is marked as its blocker, because the title text now carries an emoji prefix; downstream text processing built on the previously stable ASCII title column silently misses or mangles blocked items.
- [x] (67133e2) `src/todo/infra/cli/main.py:405` — project add/edit/archive/log redisplay the project via show_project(storage, str(id)), but resolve_project matches by NAME first, so a project whose name is the numeric string equal to the new project's id gets displayed instead.
      Scenario: Reproduced: with a project named "2" (id 1), `todo project add brand-new` creates project id 2 correctly but prints "#1  2  0/0 done" — the detail (and --json payload) of the wrong project. Same bug in project_edit (line 461), project_archive (line 477), and project_log (line 494); scripts consuming the --json output act on the wrong project's data.
- [x] (440fde1) `src/todo/tui/list_view.py:836` — The TUI editor cannot clear tags: action_edit maps an emptied 'tags:' line to tags=None (fields.get('tags') is falsy), and storage.update treats None as 'unchanged', so the user's deletion is silently ignored.
      Scenario: User presses 'e', deletes the tag list on the 'tags: a, b' line, saves — the edit appears to succeed but the item still has tags a and b; deadline on the same form CAN be cleared the same way (empty → None), making the tags behavior an inconsistent silent no-op the user only discovers later when tag filters still match the item.
- [x] (859e83a) `src/todo/infra/cli/main.py:354` — `todo block` with multiple blocker ids applies each block_todo in a loop with per-call commits, so a failure partway leaves earlier blockers committed while the command reports failure.
      Scenario: Reproduced: `todo block 3 1 999` exits 1 with 'Todo item #999 not found', but item 3 is now blocked_by [1]. A user (or script) that retries or assumes the command was rejected atomically ends up with unintended dependency state; same pattern in `unblock` (line 375).
- [x] (440fde1) `src/todo/application/commands.py:46` — edit_todo passes status straight to storage.update, bypassing _update_status, so completing an item via `todo edit N -s done` (or the TUI editor's status field) never computes/reports newly unblocked dependents — the unblock-reporting feature is bolted onto only two of the three status-change paths.
      Scenario: Reproduced: with #2 blocked by #1, `todo edit 1 -s done` exits 0 with no '🔓 #2 ... is now unblocked' warning, while `todo done 1` and `todo mv 1 done` both warn. Users completing items via edit (or the TUI e-editor changing 'status: done') silently miss the notification the feature promises, and FINAL_REPORT claims completing a blocker reports unblocked dependents.
- [x] (440fde1) `src/todo/tui/list_view.py:815` — action_edit runs subprocess.run([editor, tmp_path], check=True) with no exception handling: a nonzero editor exit raises CalledProcessError and an unset/broken $EDITOR raises FileNotFoundError, both propagating out of the action.
      Scenario: User has EDITOR set to a missing binary or quits vim with :cq — pressing 'e' in the TUI raises an unhandled exception and Textual tears down the app with a crash screen, instead of showing an error and returning to the list.

## P2 — verified, lower severity

- [x] (3508897) `src/todo/tui/list_view.py:694` — New filter status line interpolates the raw search query (and tag/project names) into Rich markup, crashing the TUI on markup metacharacters
      Scenario: Verified: in the TUI, press `/` and search for `x[/]` — the app crashes with MarkupError "closing tag '[/b]' does not match any open tag" when `search_status.update("[dim]Search:[/dim] [b]x[/][/b] ...")` renders. Same hole for tag or project names containing `[`...`]` shown via the t/p filters. User loses the running TUI session on an ordinary search string.
- [x] (2321204) `tests/test_tui.py:549` — test_stay_mode_at_last_row's final assertion `_selected_id(app) in {1, 2, 3}` accepts any item, so the cursor-clamp fallback branch it exists to cover is effectively untested
      Scenario: The test completes the last item in stay mode and then asserts only that the cursor sits on *some* item — a regression in the clamp/skip-separator fallback in _refresh_list (e.g. cursor jumping back to the first row, defeating the top-down-cleanup feature the mode was built for, or landing on the wrong group) still passes. Same weak `in {1, 2, 3}` assert at line 524 ends test_stay_mode_keeps_row_for_cleanup. The only behavior it can catch is selection becoming None; the claimed behavior ("cursor clamps to a valid item row" after the list shrinks) is unverified, so the fallback path in list_view.py lines ~665-674 could be wrong without any test failing.
- [x] (3508897) `src/todo/adapters/output.py:291` — RichOutput.print_projects interpolates the archived project's name into Rich markup (f"[dim]{name} (archived)[/dim]"), so names containing bracket sequences are parsed as markup and crash rendering.
      Scenario: Verified by repro: create a project named "x[/]y", archive it, then run `todo project list --all` in a terminal (Rich/TTY path) — print_projects raises rich.errors.MarkupError ("closing tag '[/dim]' at position 21 doesn't match any open tag") and the command crashes with a traceback instead of listing projects.
- [x] (3508897) `src/todo/tui/list_view.py:720` — The TUI detail pane builds its meta Static from f-strings that embed project_name (and tag/blocker text) into markup-parsed content, so a project name containing markup-like text crashes the whole TUI.
      Scenario: Verified by repro: `todo project add "rel[/]notes"`, assign any item to it, then run `todo ui` — as soon as the item row is highlighted (which happens automatically on mount), Static.update raises MarkupError ("auto closing tag ('[/]') has nothing to close") and the entire TUI crashes to a traceback. Same crash occurs for tags containing such text.
- [x] (dup of cluster fixed in 67133e2) `src/todo/adapters/sqlite_storage.py:300` — done_since builds items without loading blocker relations, but _item_to_dict now serializes blocked_by/blocking/is_blocked, so `todo summary --json` emits empty relation lists that contradict the rest of the CLI.
      Scenario: Verified by repro: after `block 2 1; done 1`, `todo show 1 --json` reports "blocking": [2], but `todo summary --since "7 days" --json` reports the same item with "blocking": [] and "blocked_by": [] — a consumer of the summary JSON (e.g. an AI agent deciding what got unblocked this week) reads factually wrong dependency data.
- [x] (dup of cluster fixed in 859e83a) `src/todo/infra/cli/main.py:355` — Multi-blocker `todo block A B C` commits each blocker as it goes, so an error on a later blocker id exits 1 after earlier blockers were already persisted — the error path implies nothing happened but state changed.
      Scenario: Reproduced: `todo block 1 2 999` prints 'Todo item #999 not found' and exits 1 with no item output, yet `todo show 1 --json` now shows "blocked_by": [2]. A user (or script) treating exit 1 as 'command had no effect' and re-running with a corrected list, or abandoning the operation, leaves item 1 silently blocked by 2, hiding it from `todo list --ready`.
- [x] (dup of cluster fixed in 859e83a) `src/todo/adapters/sqlite_storage.py:148` — Migrations are non-atomic and non-idempotent: _migrate runs each script with executescript (DDL auto-commits per statement) and sets user_version only afterwards, with no IF NOT EXISTS and no transaction, so an interruption between the script and the pragma leaves a half-migrated DB that can never be opened again.
      Scenario: Reproduced: create a DB where the v1 script ran but user_version was not set (process killed/crashed mid-migration). Every subsequent SqliteStorage(...) construction raises unhandled sqlite3.OperationalError 'table projects already exists', so every CLI command and the TUI traceback on startup — the user's todo database is permanently inaccessible until manually repaired.
- [x] (3508897) `src/todo/adapters/output.py:319` — print_project interpolates user text (project description, log update bodies) directly into Rich markup strings, and print_projects puts name/description raw into Table cells where markup is parsed — new in-range code repeating the exact crash class commit ba5e3ac claimed to fix.
      Scenario: Reproduced: a project description or `project log` entry containing "[/]" (or any bracketed text like "[x] done" that parses as bad markup) makes `todo project show` crash with rich.errors.MarkupError in a terminal; `todo project list` has the same issue via table cells. Text with valid-looking tags like "[red]" silently renders styled instead of literally.
- [x] (3508897) `src/todo/adapters/output.py:161` — RichOutput.print_list/print_summary still pass item.title raw into Table.add_row where Rich parses markup, so the 'Rich markup crash' FINAL_REPORT claims fixed is only fixed for the internal style-wrapper case, not for user-controlled titles; the new test_output_rich.py suite never tests a title containing brackets.
      Scenario: Reproduced: `todo add "bad [/] title"` then `todo list` in a real terminal crashes with MarkupError ("closing tag '[/]' has nothing to close"); `todo summary` crashes the same way. A title like "[red]urgent[/red]" silently renders colored instead of literal. PlainOutput (used by all CLI tests, which are non-TTY) is unaffected, which is why the added tests pass vacuously for this class.
- [x] (3508897) `src/todo/tui/list_view.py:683` — The new search-status line interpolates the raw search query, tag name, and project name into Rich markup ('[b]{...}[/b]') rendered by Static, so user text containing bracket sequences produces invalid markup.
      Scenario: Verified the markup parse raises: Text.from_markup('[dim]Search:[/dim] [b][/][/b]') → MarkupError. Typing "[/]" into the TUI search dialog, or filtering (t/p keys) onto a tag or project whose name contains such a sequence, raises MarkupError during render and crashes the TUI.
- [x] (2321204) `tests/test_tui.py:824` — test_no_refresh_when_unchanged is vacuous: it only asserts _last_data_version is unchanged after polling, which is true by construction; it never observes whether _refresh_list ran, so it cannot detect the spurious-refresh regression it names.
      Scenario: If _poll_for_external_changes were changed to unconditionally call _refresh_list (the exact bug the test claims to guard against, e.g. losing cursor position every 2 seconds), this test still passes — the claimed 'no-op when unchanged' behavior is unverified.
- [x] (2321204) `tests/test_tui.py:547` — test_stay_mode_at_last_row's final assertion `self._selected_id(app) in {1, 2, 3}` accepts any item, so it verifies nothing about stay-mode clamping — follow mode, random cursor placement, or landing on the wrong row all pass.
      Scenario: A regression where the cursor jumps to the wrong row (or stay mode silently behaves as follow mode) after completing the last item passes this test; the only failure mode it catches is the cursor landing on a separator/None, giving false confidence that the sticky-cursor edge case is covered.
- [x] (2321204) `src/todo/application/commands.py:78` — _update_status fetches every dependent three times — once for was_blocked, then twice more in the list comprehension (condition and value both call storage.get(dep_id)) — and each get() issues 4 SQL queries.
      Scenario: Completing a blocker with N dependents runs ~12N queries where N gets after the update would do; the comprehension can hoist `after = storage.get(dep_id)` once (or the storage could return dependents in one query). Wasted repeated I/O on every done/mv of a blocking item, in both CLI and TUI.
- [x] (2321204) `src/todo/adapters/sqlite_storage.py:391` — SqliteStorage.list() always fetches the entire todo_dependencies table and the id/status of every todo (two extra full-table scans) to compute blocked info, even for heavily filtered queries returning a handful of rows.
      Scenario: Every `todo list`, TUI refresh, count_tags, and list_projects call scans all rows regardless of filters; a WHERE ... IN (selected ids) on the dependency/status queries (or a JOIN with an EXISTS subquery) does the same job proportional to the result size. Cost grows with total history, not the visible list.
- [x] (2321204) `src/todo/domain/enums.py:26` — ProjectStatus.from_string is dead production code: no CLI/TUI/storage path ever calls it (grep shows the only callers are its own unit tests in test_domain.py), padding coverage without exercising real behavior.
      Scenario: 15 lines of unreachable-in-production parsing logic must be maintained and kept in sync with ProjectStatus; its tests contribute to the coverage numbers FINAL_REPORT reports while verifying code no user can reach — either wire it into a future `project` status option or delete it.
- [x] (2321204) `src/todo/adapters/sqlite_storage.py:276` — update() smuggles project_id through as str(project_id) to fit the params list typed list[str | None], storing a TEXT value that only round-trips as an integer because of SQLite column affinity.
      Scenario: Correctness currently hangs on the INTEGER affinity of the project_id column silently coercing '3' → 3; if the column type or a comparison context changes (e.g. a future strict table or a join on a TEXT-affinity column), '3' ≠ 3 and project filters/joins silently miss rows. Typing params as list[object] (as the WHERE id param at line 283 also needs) and binding the int directly removes the hazard.
- [x] (2321204) `src/todo/adapters/sqlite_storage.py:132` — ProjectList/UpdateList aliases are re-defined in sqlite_storage.py although contracts/storage.py (which this module already imports from) defines the identical aliases for the identical reason.
      Scenario: Duplicate type aliases with matching workaround comments in two files; importing ProjectList/UpdateList from todo.application.contracts.storage alongside UNSET/Unset removes the copy and keeps the protocol and adapter return types provably identical.

## Class sweeps (mandatory once per class, not per instance)

- [x] (3508897) **Markup/text safety**: audit EVERY place user-controlled text (titles,
      bodies, tag names, project names/descriptions, log bodies) reaches a
      Rich console, Rich Table cell, Textual DataTable cell, Static.update,
      Label.update, or notify(). Render via Text objects or escape — never
      f-strings into markup-interpreting sinks. Add tests with hostile input
      ("[/]", "[bold", "]]", emoji) for each sink.
- [x] (859e83a) **Atomicity**: audit every multi-step mutation (multi-blocker ops,
      migrations) for partial-application on failure.
- [x] (440fde1) **Path parity**: for every behavior promised on one mutation path
      (unblock warnings, done_at, dependency hydration), verify all paths
      that can perform the same mutation (done/mv/edit/TUI editor/TUI keys)
      share it — via a shared code path, not copies.
- [x] (67133e2) **Plain-output contract**: PlainOutput is the machine-parseable format;
      verify no decoration (emoji, markers) has leaked into it anywhere.

## Round 2 — exit-gate review wf_f78ede77-46c (2026-08-12), 10 findings

NOTE: finding [0] was INTRODUCED by the round-1 batch-rollback fix (859e83a).
Compensation-based rollback is the wrong tool here — validate-first or use a
real transaction. Treat 'my own fix' as untrusted input to this round.

- [x] (ead0578) `src/todo/adapters/output.py:185` [correctness] — block_todo_batch's all-or-nothing rollback deletes blocker relations that existed BEFORE the batch: add_blocker is INSERT OR IGNORE, so re-adding an existing blocker 'succeeds' and lands in the applied list, and the except-branch then removes it. (Reproduced: file src/todo/application/commands.py, except TodoError rollback loop at line 185.) [same root cause also at: src/todo/adapters/sqlite_storage.py:185, src/todo/application/commands.py:185, /Users/malte/airlock/todo/src/todo/application/commands.py:184, /Users/malte/airlock/todo/src/todo/application/commands.py:179]
      Scenario: Item 2 is already blocked by item 1. User runs `todo block 2 1 999` (999 doesn't exist). The command fails with 'Todo item #999 not found' and exit 1, but the pre-existing 1→2 dependency is silently deleted: `todo show 2 --json` now reports blocked_by=[] / is_blocked=false, so a task that should be gated shows up in `list --ready`. A failed command that promises 'all-or-nothing' destroys prior data. Reproduced against HEAD. (Note: correct path is src/todo/application/commands.py:185.)
- [x] (ead0578) `src/todo/application/queries.py:52` [correctness] — resolve_project resolves names before ids, so a project whose name is another project's numeric id shadows it in every ref-taking command, including the destructive `project rm`. [same root cause also at: src/todo/application/queries.py:52, src/todo/application/queries.py:53]
      Scenario: Projects exist: id 1 named "12" and id 12 named "payments". User runs `todo project rm 12` intending to delete project #12 as shown in `project list` output (which prints numeric ids in the first column). get_project_by_name("12") matches project id 1, so the wrong project is deleted — its update log is cascaded away and its todos unassigned — while project #12 survives. Same misdirection applies to `project show/edit/archive/log 12` and `--project 12` filters; only unresolvable numeric refs fall back to id lookup.
- [x] (6574842) `src/todo/tui/list_view.py:180` [correctness] — apply_editor_edit silently erases the todo body whenever the '# Body' marker line is missing from the edited buffer, contradicting its own contract that a deleted line leaves the field unchanged (_parse_editor_text at line 207 unconditionally sets fields['body'], and line 180 coerces missing body to ''). [same root cause also at: src/todo/tui/list_view.py:179]
      Scenario: User presses 'e' in the TUI on an item with a long body, and while editing deletes (or their editor template/formatting strips) the '# Body (everything below this line is the body):' comment line, keeping the body text below it. On save, the body lines no longer follow a marker, _parse_editor_text discards them, and edit_todo is called with body='' — the entire body is wiped with no warning. Reproduced against HEAD: body 'important body text' became '' after an edit that only renamed the title and dropped the marker line.
- [x] (6574842) `src/todo/tui/list_view.py:182` [correctness] — apply_editor_edit lets ValueError from Priority.from_string/Status.from_string escape uncaught into the Textual action, crashing the TUI on a mistyped priority or status in the $EDITOR buffer
      Scenario: In the TUI the user presses 'e', edits the buffer, and mistypes `priority: hgih` (or `status: doen`). action_edit calls apply_editor_edit with no exception handling; Priority.from_string raises ValueError, which propagates out of the Textual action and crashes the app with a traceback. The user's entire edit (title/body changes in the same buffer) is lost. An invalid deadline in the same buffer is silently tolerated (falls back to UNSET), so the 'robust TUI editor' hardening covers only editor-launch failures and deadline parsing, not priority/status.
- [x] (6574842) `src/todo/infra/cli/main.py:253` [correctness] — The literal ref 'none' is a clear-sentinel only in `edit --project` but a normal project name in `add --project` and all `project` subcommands, so the two mutation paths disagree about the same input.
      Scenario: User creates a project literally named 'none' (`todo project add none` succeeds; `todo add Task --project none` assigns it). Later `todo edit 1 --project none` — the only way to move an existing item into that project — instead silently CLEARS item 1's project assignment (project_id=None) and exits 0, printing output that omits project entirely so the user doesn't notice. The project named 'none' is unreachable from the edit path, and an item can be silently detached from whatever project it had.
- [x] (ead0578) `src/todo/application/commands.py:193` [correctness] — unblock_todo_batch silently succeeds when the given blocker ids are not actual blockers (or don't exist), unlike block which validates each id, so typos exit 0 with the blocker still in place
      Scenario: Item 5 is blocked by item 9. User runs `todo unblock 5 99` (typo for 9): remove_blocker deletes zero rows, no error is raised, the command exits 0 and prints item 5 — which still shows 'Blocked by: #9'. A script or user treating exit 0 as confirmation believes the dependency was removed; item 5 continues to be excluded from `todo list --ready`, while the parallel `todo block 5 99` path correctly fails with 'Todo item #99 not found'.
- [x] (6574842) `src/todo/adapters/sqlite_storage.py:152` [correctness] — _migrate has a check-then-act race on PRAGMA user_version with no IF NOT EXISTS / busy-retry protection: two processes opening a pre-migration database concurrently both see version 0 and both run the CREATE TABLE projects migration, and the loser's error propagates as an unhandled exception.
      Scenario: User upgrades the app, then a shell script fires `todo list` while the TUI (or another script) opens the same legacy DB at the same moment. Both connections read user_version=0 and executescript the same migration; the second either fails with sqlite3.OperationalError 'table projects already exists' (after the first commits) or 'database is locked' (deferred BEGIN escalating mid-script). SqliteStorage.__init__ re-raises after closing, and no CLI/TUI layer catches sqlite3 errors, so the command crashes with a raw traceback instead of retrying or reporting cleanly — a one-time-per-upgrade but user-visible hard crash.
- [x] (6574842) `src/todo/adapters/output.py:173` [correctness] — Neither RichOutput.print_item (line 173) nor PlainOutput.print_item (line 370) prints the item's project, even though both now print tags/blocked_by/blocking and the item carries project_name; every non-JSON show/add/edit/done/mv confirmation hides project assignment.
      Scenario: User runs `todo edit 1 --project infra` (or `todo add Task --project infra`, or `todo show 1`) in a terminal. The confirmation panel/plain detail lists priority, status, dates, tags, and blockers but says nothing about the project, so a wrong or accidentally cleared assignment (e.g. the '--project none' clearing path) is invisible unless the user reruns with --json. TUI detail/inspect and JSON both show 'Project:', so the CLI human output is the one mutation-feedback path where the field silently disappears.
- [x] (6574842) `src/todo/tui/list_view.py:170` [cleanup] — TUI editor silently discards an invalid deadline (maps it to UNSET) while the CLI path errors — mutation-path parity break
      Scenario: User presses 'e', changes the deadline line to '2026-13-01' or 'tomorrow', saves. apply_editor_edit swallows the ValueError and leaves the deadline unchanged with no message, so the user believes the new deadline was saved. The same input via `todo edit -d` prints "Invalid deadline ... Use YYYY-MM-DD." and exits — the two edit paths disagree and the TUI one loses user intent silently.
- [x] (6574842) `src/todo/tui/list_view.py:644` [cleanup] — TUI re-implements tag/project/priority filtering in Python although storage.list already supports tags, priority, and project_id in SQL
      Scenario: Two filter implementations now exist: CLI filters via SQL (escaped LIKE for tags, project resolved to id) while the TUI fetches everything and filters on hydrated fields (exact tag match, project matched by display name). Behavior fixes land in only one path — e.g. the LIKE-escape hardening applies to `todo list -t` but not the TUI 't' filter, and any future case-insensitivity change will split CLI and TUI results for the same filter.

## Round 3 — exit-gate review wf_15627f8b-2e7 (2026-08-12), 10 reported of 36 verified

- [x] (9f4b8b6) `src/todo/application/commands.py:154` [correctness] — block_todo's cycle check and add_blocker insert are separate autocommit steps with no transaction, so two concurrent processes can each pass the check and commit edges that together form a dependency cycle [same root cause also at: src/todo/application/commands.py:188, src/todo/application/commands.py:182, src/todo/application/commands.py:171]
      Scenario: Process A runs `todo block 1 2` while process B runs `todo block 2 1`. Each cycle-walk reads an edge-free graph, both pass, both INSERT and commit, leaving 1⇄2 mutually blocked — exactly the state DependencyError exists to prevent. Both items then show is_blocked=true forever, `todo list --ready` hides both, and nothing ever reports or repairs the cycle (the walk in block_todo has a `seen` set so later commands don't hang, they just silently operate on a corrupted graph). Same TOCTOU lets a batch race a concurrent block to build longer cycles.
- [x] (9f4b8b6) `src/todo/tui/list_view.py:140` [correctness] — _item_to_editor_text writes the title raw into a line-oriented format, so a title containing a newline is silently truncated at the first newline by _parse_editor_text on any TUI editor edit [same root cause also at: src/todo/tui/list_view.py:140, src/todo/tui/list_view.py:138, src/todo/tui/list_view.py:204, src/todo/tui/list_view.py:215]
      Scenario: Create an item with a multi-line title (`todo add "$(printf 'Fix login\nsee ticket 42')"` — the CLI accepts it and list/show render it). In the TUI press 'e', change only the priority line, save: apply_editor_edit re-parses the buffer, fields['title'] captures only 'Fix login', the continuation line has no known key and is dropped, and edit_todo persists the truncated title — silent data loss of part of the title on an edit that never touched it.
- [x] (9f4b8b6) `src/todo/adapters/sqlite_storage.py:196` [correctness] — Tags are persisted as a comma-joined string with no escaping or validation, so a tag containing a comma is silently split into multiple tags on read (latent bug in the touched add() function) [same root cause also at: src/todo/adapters/sqlite_storage.py:196, src/todo/adapters/sqlite_storage.py:196]
      Scenario: `todo add task -t 'a,b'` stores tags column 'a,b'; `todo show 1 --json` then reports tags ["a", "b"] — two tags the user never created — and `todo tags` counts phantom tags 'a' and 'b' with no tag 'a,b'. The new _like_escape work hardened tag matching against %, _ and \ but the round-trip corruption for the ',' delimiter itself remains: the stored tag can never be listed or filtered as the single tag the user typed.
- [x] (e9be38e) `src/todo/tui/list_view.py:901` [correctness] — Replaced lenient $EDITOR apply (invalid deadline formerly fell back to UNSET and still applied the other edited fields) with an all-or-nothing rejection that also deletes the temp buffer, so one field typo now destroys the user's entire edit.
      Scenario: In the TUI a user presses 'e', rewrites a long body and title in $EDITOR, and mistypes the deadline line as '2026/01/01'. On save, apply_editor_edit raises ValueError, action_edit shows a transient 'Edit rejected' toast and returns, and the finally block unlinks the temp file. Under the old code the body/title changes were saved and the old deadline kept; now every change the user typed is irrecoverably lost — there is no re-open of the buffer and no file left on disk to recover from.
- [x] (e9be38e) `src/todo/application/queries.py:57` [correctness] — resolve_project gates on ref.isdigit() but int()/SQLite reject some digit-like strings, so digit-like project refs crash with an unhandled ValueError/OverflowError instead of resolving or reporting not-found
      Scenario: `todo project add 99999999999999999999` succeeds (name validation allows it), but `todo project show 99999999999999999999` crashes with an unhandled OverflowError traceback (int exceeds SQLite's 64-bit binding range in get_project), and `todo project show ²` crashes with ValueError ('²'.isdigit() is True but int('²') raises). Both reproduced via CliRunner: exit via traceback, project permanently unreachable from show/edit/log/rm and from `todo add --project`. _resolve_project_obj_or_exit only catches ProjectNotFoundError, so the user sees a raw stack trace instead of a clean error.
- [x] (e9be38e) `src/todo/tui/list_view.py:898` [correctness] — action_edit catches only ValueError around apply_editor_edit, but edit_todo raises NotFoundError if the item was deleted by another process while $EDITOR was open, crashing the TUI [same root cause also at: src/todo/tui/list_view.py:915, src/todo/tui/list_view.py:915, src/todo/tui/list_view.py:842]
      Scenario: User presses 'e' and sits in vi for minutes; meanwhile `todo rm <id>` runs in another terminal. On save, apply_editor_edit -> edit_todo -> storage.get(item_id) raises NotFoundError, which the `except ValueError` at line 899 does not catch; the exception escapes the Textual action handler and the whole TUI crashes with a traceback, losing the user's session (the editor window is arbitrarily long, so this race is realistic).
- [x] (e9be38e) `src/todo/tui/list_view.py:882` [correctness] — action_edit runs subprocess.run([editor, tmp_path]) treating the whole $EDITOR value as one executable, so any EDITOR containing arguments (e.g. 'code --wait', 'vim -u NONE') makes TUI editing fail [same root cause also at: src/todo/tui/list_view.py:882]
      Scenario: A user with EDITOR='code --wait' (the standard VS Code setup, and a form every git/CLI tool supports via shlex-style splitting) presses 'e' in the TUI: subprocess tries to exec a program literally named 'code --wait', gets FileNotFoundError, and the TUI shows 'Editor failed: [Errno 2] No such file or directory' — the editor-based edit path is completely unusable for these users while the same EDITOR value works everywhere else on their system.
- [x] (9f4b8b6) `src/todo/application/commands.py:104` [correctness] — Replacing the plain storage.update in complete/move/edit with _tracked_update dropped the invariant that a completion either fails before mutating or succeeds and reports success: post-commit storage.get(dep_id) calls on dependents can raise NotFoundError after the item is already marked done.
      Scenario: Two processes share one database: item 7 is blocked by 5. Process A runs 'todo done 5'; after A's UPDATE commits but before its unblock-reporting loop, process B runs 'todo rm 7'. A's storage.get(7) raises NotFoundError, the CLI handler in main.py catches it, prints 'Todo item #7 not found' to stderr and exits 1 — reporting failure (and printing no item/JSON output) even though item 5 was successfully completed. Scripts checking exit status or parsing the JSON see a failed command and may retry or abort, while the mutation actually happened. The pre-618ec6b path (bare storage.update) could not fail after mutating.
- [x] (9f4b8b6) `src/todo/adapters/output.py:367` [correctness] — PlainOutput.print_list's self-declared machine-parseable one-line-per-item format breaks for titles containing newlines, which the CLI accepts unvalidated [same root cause also at: src/todo/adapters/output.py:367]
      Scenario: `todo add $'first\nsecond'` then `todo list | grep ...` (non-TTY selects PlainOutput): the comment at line 364 promises 'machine-parseable format: titles verbatim', but the embedded newline emits two physical lines for one item, so line-oriented consumers (grep/awk/cut scripts) miscount items and parse 'second' as a malformed row; print_item and print_summary have the same defect, and the trailing '<n> items' count no longer matches the number of body lines.
- [x] (e9be38e) `src/todo/infra/cli/main.py:38` [cleanup] — No CLI handler for StorageError/sqlite errors; delete()/delete_project() do not even wrap sqlite3 errors
      Scenario: Two processes share one database; one holds a write lock longer than the 5s busy_timeout. The other user's `todo rm 3` (delete has no try/except sqlite3.Error wrapper, storage.py:319) or any wrapped operation raising StorageError produces a full Python traceback instead of a one-line error and clean exit code — the CLI only catches NotFoundError/DependencyError/DuplicateProjectError.

## Round 4 — exit-gate review wf_f35e579a-2bc (2026-08-12), 10 distinct of 25 raw

- [x] (efe4faf) `src/todo/tui/list_view.py:887` [correctness] — action_edit's except tuple misses PermissionError/OSError and shlex ValueError, so common bad $EDITOR values crash the TUI [same root cause also at: src/todo/tui/list_view.py:141, src/todo/tui/list_view.py:888]
      Scenario: With EDITOR set but empty (export EDITOR=""), shlex.split("") yields [] and subprocess.run runs the non-executable 0600 temp file itself, raising PermissionError; with an unbalanced quote (EDITOR='code "'), shlex.split raises ValueError('No closing quotation') before run. Neither is in the except (FileNotFoundError, CalledProcessError, SuspendNotSupported) tuple, so pressing 'e' crashes the whole TUI with a traceback and leaks the temp file, instead of showing the 'Editor failed' notification.
- [x] (efe4faf) `src/todo/tui/list_view.py:933` [correctness] — action_delete's confirm callback (and action_done at line 848) call delete_todo/complete_todo with no NotFoundError handling, crashing the TUI on a concurrently deleted item [same root cause also at: src/todo/tui/list_view.py:933, src/todo/tui/list_view.py:848, src/todo/tui/list_view.py:933, src/todo/tui/list_view.py:848]
      Scenario: User presses 'x' on item #5; while the ConfirmDialog sits open (arbitrarily long), another process runs 'todo rm 5'. Pressing 'y' calls delete_todo -> storage.get raises NotFoundError, which is uncaught in the callback, so the TUI crashes with a traceback. Same for 'd' (action_done, line 848) within the 2-second poll window. Sibling actions (edit/inspect/status_next) all catch NotFoundError, so this is an inconsistently missing guard on two mutation paths.
- [x] (efe4faf) `src/todo/adapters/sqlite_storage.py:192` [correctness] — transaction()'s BEGIN IMMEDIATE raises raw sqlite3.OperationalError (never wrapped in StorageError), and no TUI mutation path handles it — CLI parity exists only via _SafeGroup, which the TUI lacks [same root cause also at: src/todo/tui/list_view.py:914]
      Scenario: Reproducible: open the db in another terminal with `sqlite3 <db> 'BEGIN IMMEDIATE'` held open (or any writer holding the lock >5s busy_timeout), then in the TUI press 'd', '>', or save an editor edit. transaction() raises sqlite3.OperationalError('database is locked'); it is not a TodoError, so even _apply_edited_buffer's `except (ValueError, TodoError)` misses it, and the whole TUI crashes with a traceback, while the identical CLI operation prints 'Database error: ...' and exits cleanly.
- [x] (ca68b63) `src/todo/tui/list_view.py:146` [correctness] — Editor round-trip silently truncates multi-line titles left in the database by the pre-normalization version (no data migration normalizes existing rows)
      Scenario: A title created as 'part one\npart two' under the baseline version (which stored titles verbatim) survives the schema migration unchanged. User opens the item in $EDITOR to change its status: _item_to_editor_text writes 'title: part one\npart two\npriority:...', _parse_editor_text keeps only 'part one' and discards the stray line, and saving updates title to 'part one' — the rest of the title is permanently lost with no warning. The same legacy rows also break PlainOutput.print_list's one-line-per-item format.
- [x] (ca68b63) `src/todo/application/commands.py:243` [correctness] — Project names bypass the single-line normalization enforced for titles: newlines, tabs, and leading/trailing whitespace are stored verbatim, breaking the plain machine-parseable output [same root cause also at: src/todo/application/commands.py:243]
      Scenario: todo project add $'sprint\n42' succeeds. Piped 'todo project list' (PlainOutput.print_projects, one-row-per-project contract) then emits two physical lines for one project, and 'todo project show' / plain print_item's 'Project: ...' line becomes multi-line, breaking any script parsing the documented plain format. Also '  work ' and 'work' become distinct near-duplicate projects since names are never stripped, while todo titles get exactly this normalization at the same boundary.
- [x] (ca68b63) `src/todo/application/commands.py:157` [correctness] — delete_todo silently unblocks dependents: cascade removal of dependency edges emits none of the unblock warnings that every completion path guarantees
      Scenario: Item #2 is blocked only by #1. 'todo done 1' prints '🔓 #2 ... is now unblocked', but 'todo rm 1' removes the edge via ON DELETE CASCADE and prints only 'Deleted #1' — #2 becomes actionable with no notification in CLI or TUI, despite _tracked_update's stated invariant that no path may silently miss an unblock; the two mutation paths that unblock an item behave inconsistently.
- [x] (efe4faf) `src/todo/tui/list_view.py:189` [correctness] — Blanking the title line in the editor buffer is silently ignored ('' or None) instead of erroring, so the save partially applies
      Scenario: User edits the buffer to 'title:' (empty) while also changing status. fields['title'] == '' collapses to None via 'or None', so the status change is applied but the title silently keeps its old value — no error, no kept-buffer notification. This contradicts the module's own contract enforced two branches up for deadline ('bad input errors, never a silent no-op'): an empty title should raise like _normalize_title does on every other path, not partially apply the edit.
- [x] (ca68b63) `src/todo/infra/cli/main.py:129` [correctness] — The rewritten add/list commands guard project resolution with truthiness ('if project_ref else None') while edit guards with 'is not None', so an empty-string --project is silently treated as 'no project' on add/list but is an error on edit — a dropped error path relative to the id-first resolution contract.
      Scenario: A script runs `todo add "deploy fix" --project "$PROJ"` with $PROJ unset/empty. Instead of failing like `todo edit 5 --project ""` does (Project '' not found, exit 1), the todo is silently created unassigned, and `todo list --project ""` silently lists all todos instead of erroring — the item lands outside the intended project with no diagnostic.
- [x] (ca68b63) `src/todo/tui/list_view.py:88` [cleanup] — TUI re-implements _priority_label/_status_icon/_relative_age/_deadline_str from adapters/output.py
      Scenario: Four presentation helpers are copy-pasted from src/todo/adapters/output.py (lines 13-58, 486-509) and have already drifted: output.py renders overdue deadlines as '(3d overdue)' while list_view.py renders '(3d late)', and the months branch differs. tests/test_architecture.py explicitly allows tui -> todo.adapters imports, so the TUI can import the existing helpers; any future fix (e.g. the months rounding) applied to one copy silently misses the other, making CLI and TUI disagree.
- [x] (ca68b63) `src/todo/application/queries.py:79` [cleanup] — list_projects loads and dependency-hydrates every todo ever created just to count open/done [same root cause also at: src/todo/tui/list_view.py:653, src/todo/tui/list_view.py:979]
      Scenario: storage.list(include_done=True) fetches the whole todos table, then _hydrate_dependencies issues extra queries and builds full TodoItem objects, all discarded except project_id/is_done. 'todo project list' (and the TUI paths below) therefore scales with total history size including done items; a single 'SELECT project_id, status, COUNT(*) FROM todos GROUP BY 1,2' (or a storage-level count method) does the same work in one indexed query with no hydration.

## Round 5 — exit-gate review wf_5bd2056b-192 (2026-08-12), 9 distinct

- [x] (see round-5 commit) `src/todo/adapters/sqlite_storage.py:75` [correctness] — v3 migration's project-name normalization can violate the projects.name UNIQUE constraint, making the database permanently unopenable
      Scenario: A pre-v3 database contains two legacy projects whose names normalize to the same string (e.g. 'alpha\nbeta' and 'alpha beta' — exactly the rows this migration exists to clean). On next open, the v3 UPDATE raises sqlite3.IntegrityError: UNIQUE constraint failed: projects.name; _apply_migration only handles OperationalError, so __init__ rolls back and re-raises. Reproduced: every subsequent open fails identically, so every CLI command prints 'Database error: UNIQUE constraint failed: projects.name' and the TUI cannot start — the user is permanently locked out of all their data with no recovery path.
- [x] (see round-5 commit) `src/todo/tui/list_view.py:475` [correctness] — BlockDialog.on_submit does not catch StorageError, so a locked database crashes the whole TUI instead of showing the inline error [same root cause also at: src/todo/tui/list_view.py:475, src/todo/tui/list_view.py:475]
      Scenario: Another process holds a write transaction for longer than the 5s busy timeout (e.g. a stuck script or an open `sqlite3 todo.db` shell with BEGIN IMMEDIATE). User presses 'b' in the TUI and submits a blocker id: block_todo's storage.transaction() raises StorageError('Failed to start transaction: database is locked'), which is not in the dialog's (NotFoundError, DependencyError, ValueError) catch tuple. Reproduced at the exception level: the StorageError escapes the Input.Submitted handler, so Textual tears down the running app with a traceback — the TUI session dies instead of displaying the error in the dialog's error label like every other guarded mutation path.
- [x] (see round-5 commit) `src/todo/tui/list_view.py:410` [correctness] — NewItemDialog.action_save calls add_todo with no error handling, so a locked database crashes the whole TUI when saving a new item [same root cause also at: src/todo/tui/list_view.py:410, src/todo/tui/list_view.py:410]
      Scenario: Another process holds a write lock past the 5s busy timeout. User presses 'n', fills in the new-item form, and hits Enter on the tags field: SqliteStorage.add wraps the sqlite3.OperationalError as StorageError('Failed to add todo: database is locked'), and action_save has no try/except at all (unlike action_done/action_delete/status moves, which catch TodoError). Reproduced at the exception level: the StorageError propagates out of the message handler and Textual crashes the app — the user loses the TUI session and the item they just typed, instead of seeing an error notification.
- [x] (see round-5 commit) `src/todo/tui/list_view.py:871` [cleanup] — Editor buffer read-back `open(tmp_path)` is unguarded; an editor that removes or renames the temp file crashes the TUI
      Scenario: If $EDITOR is a wrapper script that moves/deletes the buffer file (or an editor crashes after unlinking its target) while still exiting 0, subprocess.run succeeds but `open(tmp_path)` on line 871 raises FileNotFoundError, which nothing catches, crashing the TUI. The surrounding editor flow was explicitly hardened round 4 (ValueError/OSError/CalledProcessError/SuspendNotSupported around the subprocess call at 859-869), but the read-back two lines later has no OSError guard, so the 'TUI survives editor misconfiguration' invariant has a hole.
- [x] (see round-5 commit) `src/todo/adapters/sqlite_storage.py:72` [correctness] — v3 legacy-title migration collapses whitespace with a single non-recursive REPLACE('  ',' '), so titles whose newlines produced runs of 3+ whitespace characters keep double spaces after migration, violating the single-line normalized contract the migration exists to restore.
      Scenario: A legacy (pre-v3) database contains a title like 'part one\n\n\npart two' (blank lines between paragraphs pasted into a title before normalization existed). After upgrading, the migration produces 'part one  part two' (verified in SQLite: 'a\n\n\nb' -> 'a  b', and 'x\n \ny' -> 'x  y') instead of the 'part one part two' that _normalize_title guarantees everywhere else. The user visibly sees a title with stray double spaces in list/show/TUI output, and the first time they save any unrelated editor edit of that item, the title silently changes again (normalized to single spaces), showing the migration did not actually converge the data to the advertised format.
- [x] (see round-5 commit) `src/todo/application/commands.py:207` [cleanup] — block_todo's cycle check walks the dependency graph via full storage.get() per node, and block_todo_batch reruns the whole BFS per blocker
      Scenario: Each BFS node costs a full item hydration (item row + projects JOIN, blockers query, blocking query, blocker-status query — 4 statements in SqliteStorage.get) just to read `.blocking`, and `todo block 1 2 3 4` repeats the complete traversal once per blocker id. On a few hundred items with chains this is thousands of queries inside one IMMEDIATE transaction, holding the write lock (and blocking the TUI's mutations) for the duration. A single `SELECT blocked_id FROM todo_dependencies WHERE blocker_id = ?` per node — or one recursive CTE reachability query, or loading the whole edge table once per batch — does the same check in a handful of statements.
- [x] (see round-5 commit) `src/todo/application/queries.py:135` [cleanup] — count_tags loads every todo (done included) with full dependency hydration just to count tag strings
      Scenario: count_tags calls storage.list(include_done=True), which runs the LEFT JOIN select plus _hydrate_dependencies (dependency edges + blocker statuses) and builds full TodoItem dataclasses, only to read `.tags`. The TUI runs this on every 't' keypress (action_cycle_tag), so cycling the tag filter re-reads the entire todos and todo_dependencies tables per keystroke. A `SELECT tags FROM todos` (or a storage-level tag_counts() like the existing project_counts()) gives the same result for the cost of one column scan.
- [ ] `FINDINGS.md:1` [cleanup] — Review-loop process artifacts (FINDINGS.md, LOOP.md, LOOP2.md, FINAL_REPORT.md) are committed to the repo root
      Scenario: FINDINGS.md, LOOP.md, LOOP2.md, and FINAL_REPORT.md are internal scratch logs of the autonomous fix loop (finding queues, iteration notes), not project documentation. They ship in the repository next to README.md/PRD.md/ROADMAP.md, confusing readers about which documents are authoritative and going stale immediately (FINDINGS.md describes bugs that are already fixed). They should be deleted or moved out of version control now that the campaign is done.
- [x] (see round-5 commit) `src/todo/adapters/sqlite_storage.py:366` [cleanup] — SqliteStorage.delete and delete_project are the only mutations that don't wrap sqlite3.Error as StorageError
      Scenario: add/update/add_blocker/remove_blocker/add_project/update_project/add_project_update all wrap sqlite3.Error into StorageError (the contract the TUI's `except TodoError` guards and the round-4 'sqlite errors wrapped as StorageError' fix rely on), but delete (line 366) and delete_project (line 601) execute raw. Today they happen to run inside transaction() whose BEGIN fails first on lock contention, but any caller invoking storage.delete outside a transaction — or an I/O error mid-delete — surfaces a raw sqlite3.OperationalError that StorageError-based handlers miss, crashing instead of showing 'Database error'. Wrapping these two like their siblings closes the inconsistency.

## Triage log

- Round-5 finding `FINDINGS.md:1` (process artifacts committed to repo root):
  TRIAGED — intentional, not a defect. LOOP.md/LOOP2.md/ROADMAP.md/FINDINGS.md/
  FINAL_REPORT.md are the audit trail the user explicitly asked the autonomous
  loops to maintain ("when I come back... I want to see this"), and the user
  reads them to follow progress. They are documentation of process, reviewed
  and committed deliberately; removal would be done at the user's request at
  the end, not silently mid-loop.

(Only for findings believed to be false positives: the finding, the
reproduction attempt that failed, and why the code is actually correct.
Anything without a failed reproduction attempt gets fixed, not triaged.)

## Status log

- iter 1: markup class closed, 8 findings (3508897). 225 tests green.
- iter 12: round-5 queue closed — Python migration v3 (collision-safe, exact normalization), dialog StorageError guards, buffer read-back guard, delete wrapping, edge-walk + tag-scan perf. 303 tests green. QUEUE EMPTY (round 5); artifacts finding triaged as intentional.
- iter 11: round-4 queue closed — migration v3 legacy normalization, project-name single-line contract, delete unblock parity, empty --project parity, helper dedupe, SQL project counts (ca68b63). 297 tests green. QUEUE EMPTY (round 4).
- iter 10: round-4 partial — TUI error-handling class (editor config, races, locked db), empty-title contract (efe4faf). 289 tests green.
- iter 9: round-3 queue closed — title/tag normalization, transactional graph mutations, completion invariant (9f4b8b6, b747c39). 282 tests green. QUEUE EMPTY (round 3). Process note: 9f4b8b6 landed with one red test (footer false-match in a NEW test, product behavior was correct) because piping pytest through tail masked its exit code; fixed in b747c39 and gates now preserve exit codes.
- iter 8: round-3 partial — editor robustness (shlex, buffer kept on reject, NotFoundError), project-ref crash guards, CLI db-error handler (e9be38e). 267 tests green.
- iter 7: round-2 queue closed — editor body/validation, 'none' reserved, migration race, project in show, SQL filters (6574842). 257 tests green. QUEUE EMPTY (round 2).
- iter 6: round-2 review NOT clean (10 findings, one introduced by round-1 fix). Fixed batch data loss, unblock validation, id-first refs (ead0578). 247 tests green.
- iter 5: P2 tail closed — exact cursor assertions, observed-refresh test, hydration scoping, dead code, int binding, alias dedup (2321204). 243 tests green. QUEUE EMPTY.
- iter 4: project display, summary hydration, plain-output purity (67133e2). 243 tests green.
- iter 3: path parity — edit-to-done warnings, editor tag clearing, editor error handling (440fde1). 240 tests green.
- iter 2: atomicity class closed — atomic migrations, batch blockers (859e83a). 230 tests green.

(One line per iteration: which findings closed, commit hashes, gate status.)
