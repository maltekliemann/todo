# Findings queue — verified by adversarial review (run wf_315cd7bd-fb1, 2026-08-12)

31 findings, all independently CONFIRMED against the code. P1 = the ten
reported as severe; P2 = the rest. Every entry gets: a test that FAILS on
the current code first, then the fix, then the class-sweep check (below).

## P1 — severe

- [x] (3508897) `src/todo/tui/list_view.py:649` — TUI crashes on launch with MarkupError when any item title contains Rich markup metacharacters, because row cells are passed to DataTable as plain strings which it parses as markup
      Scenario: Verified: `todo add "Fix [/] thing"` then `todo ui` crashes immediately on startup (DataTable.default_cell_formatter -> Text.from_markup -> MarkupError "closing tag '[/]' has nothing to close"). The TUI is completely unusable until the offending item is deleted via the CLI; any title with an unbalanced closing tag like "[/]" or "[/b]" triggers it. The is_blocked branch wraps cells in Text (safe) but the common unblocked branch passes raw str.
- [x] (3508897) `src/todo/adapters/output.py:156` — RichOutput.print_list (and print_project's description/log at line 319) render user text as Rich markup, so `todo list` on a TTY crashes for titles containing markup; the in-range 'markup crash fix' (_styled) only fixed the empty-style case
      Scenario: Verified: RichOutput.print_list with an item titled "Fix [/] thing" raises MarkupError (table cells are strings, parsed as markup). A user who runs `todo add "Fix [/] thing"` gets a traceback from every subsequent `todo list`/`todo summary` in a terminal until they edit the item, and `todo project show` crashes likewise on a description or log body containing `[/]`. Piped/plain output works, which hides the bug from all CLI tests (CliRunner is not a TTY) and from test_output_rich.py, which never uses markup-metachar text.
- [ ] `src/todo/infra/cli/main.py:405` — project add/edit/archive/log re-resolve the project via show_project(storage, str(id)), which resolves by NAME first, so they display (and emit as --json) a different project whose name equals the numeric id
      Scenario: Verified: with an existing project named "2", `todo project add foo --json` succeeds but prints the JSON detail of project "2" (id 1) instead of the just-created "foo" (id 2). Any script consuming the output records the wrong project id/name; `project edit`, `project archive`, and `project log` have the same bug since they also round-trip the known integer id through the name-first resolver instead of calling show_project with the Project they already hold.
- [x] (859e83a) `src/todo/infra/cli/main.py:354` — `todo block ID B1 B2...` applies blockers one-by-one with no rollback, so a failure partway leaves earlier edges committed while reporting failure
      Scenario: Verified: `todo block 2 1 999` prints "Todo item #999 not found" and exits 1, but the 1-blocks-2 edge was already committed — `todo show 2 --json` now shows blocked_by [1] and is_blocked true. A user who sees the error, fixes the typo, and expects a clean state (or a script retrying the whole command) ends up with half-applied dependency state that the exit code claims never happened; the same loop shape affects `unblock` with multiple ids.
- [ ] `src/todo/infra/cli/main.py:405` — project_add re-resolves the newly created project via show_project(storage, str(created.id)), and resolve_project prefers name lookup over id, so a project literally named after the new project's numeric id is displayed instead of the one just created.
      Scenario: Verified by repro: with a project named "2" (id 1) already present, `todo project add foo --json` succeeds in creating "foo" (id 2) but prints the JSON/detail of project id 1 named "2" — a script consuming the output records the wrong project id/name. The same name-shadows-id resolution makes `todo project show 2`, edit, archive, log, and rm target the wrong project, so `todo project rm 2` intending id 2 would delete a different project (data loss).
- [x] (859e83a) `src/todo/infra/cli/main.py:354` — The block command applies blocker edges one at a time inside the loop and exits on the first error, leaving earlier edges committed even though the command reports failure.
      Scenario: Verified by repro: `todo block 3 1 999` prints "Todo item #999 not found" and exits 1, but item 3 is now silently blocked by item 1 (`show 3 --json` reports blocked_by [1]). A user or script that treats exit 1 as "nothing happened" and retries or abandons the operation ends up with half-applied dependency state; the same partial commit happens when a later blocker in the list would form a cycle.
- [x] (859e83a) `src/todo/adapters/sqlite_storage.py:149` — Schema setup lost its old idempotence guard: migrations run CREATE TABLE without IF NOT EXISTS and the PRAGMA user_version bump is a separate non-atomic step after executescript, so a partially-applied migration state is fatal on every subsequent open.
      Scenario: A crash/kill/disk-full between `executescript(script)` (which autocommits DDL statement-by-statement) and the `PRAGMA user_version` bump leaves the projects table created with user_version still 0. Reproduced: opening such a DB makes every `todo` command die with an uncaught traceback `sqlite3.OperationalError: table projects already exists` — the database is permanently unusable until manually repaired. The pre-change init (all CREATE TABLE IF NOT EXISTS) was safe to re-run on any state.
- [ ] `src/todo/adapters/sqlite_storage.py:306` — done_since() never hydrates blocker relations, so summary output reports blocked_by/blocking as empty even when the get()/list() paths report real edges for the same item.
      Scenario: Reproduced: with item #1 done and still recorded as blocking #2, `todo show 1 --json` prints "blocking": [2] but `todo summary --since '1 days' --json` prints "blocking": [] and "blocked_by": [] for the same item — JSON consumers of the summary get factually wrong dependency data that contradicts the rest of the CLI.
- [ ] `src/todo/adapters/output.py:351` — PlainOutput.print_list no longer prints titles verbatim: blocked items get a '\U0001f6a7 ' (🚧) prefix injected into the plain formatter that exists specifically for machine-friendly piped output.
      Scenario: A script that pipes `todo list` (non-TTY selects PlainOutput) and greps/parses the title column matches an item while it is unblocked but stops matching the moment another item is marked as its blocker, because the title text now carries an emoji prefix; downstream text processing built on the previously stable ASCII title column silently misses or mangles blocked items.
- [ ] `src/todo/infra/cli/main.py:405` — project add/edit/archive/log redisplay the project via show_project(storage, str(id)), but resolve_project matches by NAME first, so a project whose name is the numeric string equal to the new project's id gets displayed instead.
      Scenario: Reproduced: with a project named "2" (id 1), `todo project add brand-new` creates project id 2 correctly but prints "#1  2  0/0 done" — the detail (and --json payload) of the wrong project. Same bug in project_edit (line 461), project_archive (line 477), and project_log (line 494); scripts consuming the --json output act on the wrong project's data.
- [x] (440fde1) `src/todo/tui/list_view.py:836` — The TUI editor cannot clear tags: action_edit maps an emptied 'tags:' line to tags=None (fields.get('tags') is falsy), and storage.update treats None as 'unchanged', so the user's deletion is silently ignored.
      Scenario: User presses 'e', deletes the tag list on the 'tags: a, b' line, saves — the edit appears to succeed but the item still has tags a and b; deadline on the same form CAN be cleared the same way (empty → None), making the tags behavior an inconsistent silent no-op the user only discovers later when tag filters still match the item.
- [x] (859e83a) `src/todo/infra/cli/main.py:354` — `todo block` with multiple blocker ids applies each block_todo in a loop with per-call commits, so a failure partway leaves earlier blockers committed while the command reports failure.
      Scenario: Reproduced: `todo block 3 1 999` exits 1 with 'Todo item #999 not found', but item 3 is now blocked_by [1]. A user (or script) that retries or assumes the command was rejected atomically ends up with unintended dependency state; same pattern in `unblock` (line 375).
- [x] (440fde1) `src/todo/application/commands.py:46` — edit_todo passes status straight to storage.update, bypassing _update_status, so completing an item via `todo edit N -s done` (or the TUI editor's status field) never computes/reports newly unblocked dependents — the unblock-reporting feature is bolted onto only two of the three status-change paths.
      Scenario: Reproduced: with #2 blocked by #1, `todo edit 1 -s done` exits 0 with no '🔓 #2 ... is now unblocked' warning, while `todo done 1` and `todo mv 1 done` both warn. Users completing items via edit (or the TUI e-editor changing 'status: done') silently miss the notification the feature promises, and FINAL_REPORT claims completing a blocker reports unblocked dependents.
- [x] (440fde1) `src/todo/tui/list_view.py:815` — action_edit runs subprocess.run([editor, tmp_path], check=True) with no exception handling: a nonzero editor exit raises CalledProcessError and an unset/broken $EDITOR raises FileNotFoundError, both propagating out of the action.
      Scenario: User has EDITOR set to a missing binary or quits vim with :cq — pressing 'e' in the TUI raises an unhandled exception and Textual tears down the app with a crash screen, instead of showing an error and returning to the list.

## P2 — verified, lower severity

- [x] (3508897) `src/todo/tui/list_view.py:694` — New filter status line interpolates the raw search query (and tag/project names) into Rich markup, crashing the TUI on markup metacharacters
      Scenario: Verified: in the TUI, press `/` and search for `x[/]` — the app crashes with MarkupError "closing tag '[/b]' does not match any open tag" when `search_status.update("[dim]Search:[/dim] [b]x[/][/b] ...")` renders. Same hole for tag or project names containing `[`...`]` shown via the t/p filters. User loses the running TUI session on an ordinary search string.
- [ ] `tests/test_tui.py:549` — test_stay_mode_at_last_row's final assertion `_selected_id(app) in {1, 2, 3}` accepts any item, so the cursor-clamp fallback branch it exists to cover is effectively untested
      Scenario: The test completes the last item in stay mode and then asserts only that the cursor sits on *some* item — a regression in the clamp/skip-separator fallback in _refresh_list (e.g. cursor jumping back to the first row, defeating the top-down-cleanup feature the mode was built for, or landing on the wrong group) still passes. Same weak `in {1, 2, 3}` assert at line 524 ends test_stay_mode_keeps_row_for_cleanup. The only behavior it can catch is selection becoming None; the claimed behavior ("cursor clamps to a valid item row" after the list shrinks) is unverified, so the fallback path in list_view.py lines ~665-674 could be wrong without any test failing.
- [x] (3508897) `src/todo/adapters/output.py:291` — RichOutput.print_projects interpolates the archived project's name into Rich markup (f"[dim]{name} (archived)[/dim]"), so names containing bracket sequences are parsed as markup and crash rendering.
      Scenario: Verified by repro: create a project named "x[/]y", archive it, then run `todo project list --all` in a terminal (Rich/TTY path) — print_projects raises rich.errors.MarkupError ("closing tag '[/dim]' at position 21 doesn't match any open tag") and the command crashes with a traceback instead of listing projects.
- [x] (3508897) `src/todo/tui/list_view.py:720` — The TUI detail pane builds its meta Static from f-strings that embed project_name (and tag/blocker text) into markup-parsed content, so a project name containing markup-like text crashes the whole TUI.
      Scenario: Verified by repro: `todo project add "rel[/]notes"`, assign any item to it, then run `todo ui` — as soon as the item row is highlighted (which happens automatically on mount), Static.update raises MarkupError ("auto closing tag ('[/]') has nothing to close") and the entire TUI crashes to a traceback. Same crash occurs for tags containing such text.
- [ ] `src/todo/adapters/sqlite_storage.py:300` — done_since builds items without loading blocker relations, but _item_to_dict now serializes blocked_by/blocking/is_blocked, so `todo summary --json` emits empty relation lists that contradict the rest of the CLI.
      Scenario: Verified by repro: after `block 2 1; done 1`, `todo show 1 --json` reports "blocking": [2], but `todo summary --since "7 days" --json` reports the same item with "blocking": [] and "blocked_by": [] — a consumer of the summary JSON (e.g. an AI agent deciding what got unblocked this week) reads factually wrong dependency data.
- [ ] `src/todo/infra/cli/main.py:355` — Multi-blocker `todo block A B C` commits each blocker as it goes, so an error on a later blocker id exits 1 after earlier blockers were already persisted — the error path implies nothing happened but state changed.
      Scenario: Reproduced: `todo block 1 2 999` prints 'Todo item #999 not found' and exits 1 with no item output, yet `todo show 1 --json` now shows "blocked_by": [2]. A user (or script) treating exit 1 as 'command had no effect' and re-running with a corrected list, or abandoning the operation, leaves item 1 silently blocked by 2, hiding it from `todo list --ready`.
- [ ] `src/todo/adapters/sqlite_storage.py:148` — Migrations are non-atomic and non-idempotent: _migrate runs each script with executescript (DDL auto-commits per statement) and sets user_version only afterwards, with no IF NOT EXISTS and no transaction, so an interruption between the script and the pragma leaves a half-migrated DB that can never be opened again.
      Scenario: Reproduced: create a DB where the v1 script ran but user_version was not set (process killed/crashed mid-migration). Every subsequent SqliteStorage(...) construction raises unhandled sqlite3.OperationalError 'table projects already exists', so every CLI command and the TUI traceback on startup — the user's todo database is permanently inaccessible until manually repaired.
- [x] (3508897) `src/todo/adapters/output.py:319` — print_project interpolates user text (project description, log update bodies) directly into Rich markup strings, and print_projects puts name/description raw into Table cells where markup is parsed — new in-range code repeating the exact crash class commit ba5e3ac claimed to fix.
      Scenario: Reproduced: a project description or `project log` entry containing "[/]" (or any bracketed text like "[x] done" that parses as bad markup) makes `todo project show` crash with rich.errors.MarkupError in a terminal; `todo project list` has the same issue via table cells. Text with valid-looking tags like "[red]" silently renders styled instead of literally.
- [x] (3508897) `src/todo/adapters/output.py:161` — RichOutput.print_list/print_summary still pass item.title raw into Table.add_row where Rich parses markup, so the 'Rich markup crash' FINAL_REPORT claims fixed is only fixed for the internal style-wrapper case, not for user-controlled titles; the new test_output_rich.py suite never tests a title containing brackets.
      Scenario: Reproduced: `todo add "bad [/] title"` then `todo list` in a real terminal crashes with MarkupError ("closing tag '[/]' has nothing to close"); `todo summary` crashes the same way. A title like "[red]urgent[/red]" silently renders colored instead of literal. PlainOutput (used by all CLI tests, which are non-TTY) is unaffected, which is why the added tests pass vacuously for this class.
- [x] (3508897) `src/todo/tui/list_view.py:683` — The new search-status line interpolates the raw search query, tag name, and project name into Rich markup ('[b]{...}[/b]') rendered by Static, so user text containing bracket sequences produces invalid markup.
      Scenario: Verified the markup parse raises: Text.from_markup('[dim]Search:[/dim] [b][/][/b]') → MarkupError. Typing "[/]" into the TUI search dialog, or filtering (t/p keys) onto a tag or project whose name contains such a sequence, raises MarkupError during render and crashes the TUI.
- [ ] `tests/test_tui.py:824` — test_no_refresh_when_unchanged is vacuous: it only asserts _last_data_version is unchanged after polling, which is true by construction; it never observes whether _refresh_list ran, so it cannot detect the spurious-refresh regression it names.
      Scenario: If _poll_for_external_changes were changed to unconditionally call _refresh_list (the exact bug the test claims to guard against, e.g. losing cursor position every 2 seconds), this test still passes — the claimed 'no-op when unchanged' behavior is unverified.
- [ ] `tests/test_tui.py:547` — test_stay_mode_at_last_row's final assertion `self._selected_id(app) in {1, 2, 3}` accepts any item, so it verifies nothing about stay-mode clamping — follow mode, random cursor placement, or landing on the wrong row all pass.
      Scenario: A regression where the cursor jumps to the wrong row (or stay mode silently behaves as follow mode) after completing the last item passes this test; the only failure mode it catches is the cursor landing on a separator/None, giving false confidence that the sticky-cursor edge case is covered.
- [ ] `src/todo/application/commands.py:78` — _update_status fetches every dependent three times — once for was_blocked, then twice more in the list comprehension (condition and value both call storage.get(dep_id)) — and each get() issues 4 SQL queries.
      Scenario: Completing a blocker with N dependents runs ~12N queries where N gets after the update would do; the comprehension can hoist `after = storage.get(dep_id)` once (or the storage could return dependents in one query). Wasted repeated I/O on every done/mv of a blocking item, in both CLI and TUI.
- [ ] `src/todo/adapters/sqlite_storage.py:391` — SqliteStorage.list() always fetches the entire todo_dependencies table and the id/status of every todo (two extra full-table scans) to compute blocked info, even for heavily filtered queries returning a handful of rows.
      Scenario: Every `todo list`, TUI refresh, count_tags, and list_projects call scans all rows regardless of filters; a WHERE ... IN (selected ids) on the dependency/status queries (or a JOIN with an EXISTS subquery) does the same job proportional to the result size. Cost grows with total history, not the visible list.
- [ ] `src/todo/domain/enums.py:26` — ProjectStatus.from_string is dead production code: no CLI/TUI/storage path ever calls it (grep shows the only callers are its own unit tests in test_domain.py), padding coverage without exercising real behavior.
      Scenario: 15 lines of unreachable-in-production parsing logic must be maintained and kept in sync with ProjectStatus; its tests contribute to the coverage numbers FINAL_REPORT reports while verifying code no user can reach — either wire it into a future `project` status option or delete it.
- [ ] `src/todo/adapters/sqlite_storage.py:276` — update() smuggles project_id through as str(project_id) to fit the params list typed list[str | None], storing a TEXT value that only round-trips as an integer because of SQLite column affinity.
      Scenario: Correctness currently hangs on the INTEGER affinity of the project_id column silently coercing '3' → 3; if the column type or a comparison context changes (e.g. a future strict table or a join on a TEXT-affinity column), '3' ≠ 3 and project filters/joins silently miss rows. Typing params as list[object] (as the WHERE id param at line 283 also needs) and binding the int directly removes the hazard.
- [ ] `src/todo/adapters/sqlite_storage.py:132` — ProjectList/UpdateList aliases are re-defined in sqlite_storage.py although contracts/storage.py (which this module already imports from) defines the identical aliases for the identical reason.
      Scenario: Duplicate type aliases with matching workaround comments in two files; importing ProjectList/UpdateList from todo.application.contracts.storage alongside UNSET/Unset removes the copy and keeps the protocol and adapter return types provably identical.

## Class sweeps (mandatory once per class, not per instance)

- [x] (3508897) **Markup/text safety**: audit EVERY place user-controlled text (titles,
      bodies, tag names, project names/descriptions, log bodies) reaches a
      Rich console, Rich Table cell, Textual DataTable cell, Static.update,
      Label.update, or notify(). Render via Text objects or escape — never
      f-strings into markup-interpreting sinks. Add tests with hostile input
      ("[/]", "[bold", "]]", emoji) for each sink.
- [x] (859e83a) **Atomicity**: audit every multi-step mutation (multi-blocker ops,
      migrations) for partial-application on failure.
- [x] (440fde1) **Path parity**: for every behavior promised on one mutation path
      (unblock warnings, done_at, dependency hydration), verify all paths
      that can perform the same mutation (done/mv/edit/TUI editor/TUI keys)
      share it — via a shared code path, not copies.
- [ ] **Plain-output contract**: PlainOutput is the machine-parseable format;
      verify no decoration (emoji, markers) has leaked into it anywhere.

## Triage log

(Only for findings believed to be false positives: the finding, the
reproduction attempt that failed, and why the code is actually correct.
Anything without a failed reproduction attempt gets fixed, not triaged.)

## Status log

- iter 1: markup class closed, 8 findings (3508897). 225 tests green.
- iter 3: path parity — edit-to-done warnings, editor tag clearing, editor error handling (440fde1). 240 tests green.
- iter 2: atomicity class closed — atomic migrations, batch blockers (859e83a). 230 tests green.

(One line per iteration: which findings closed, commit hashes, gate status.)
